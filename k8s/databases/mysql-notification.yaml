apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: terra-notification-mysql-pvc
  namespace: terrabia
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: terra-notification-db
  namespace: terrabia
spec:
  replicas: 1
  selector:
    matchLabels:
      app: terra-notification-db
  template:
    metadata:
      labels:
        app: terra-notification-db
    spec:
      containers:
      - name: mysql
        image: mysql:8.0
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: mysql-root-password
        - name: MYSQL_DATABASE
          value: "terra_notification_db"
        - name: MYSQL_USER
          value: "terra_notification_user"
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: mysql-password
        ports:
        - containerPort: 3306
        volumeMounts:
        - name: mysql-data
          mountPath: /var/lib/mysql
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
      volumes:
      - name: mysql-data
        persistentVolumeClaim:
          claimName: terra-notification-mysql-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: terra-notification-db
  namespace: terrabia
spec:
  selector:
    app: terra-notification-db
  ports:
  - port: 3306
    targetPort: 3306
  type: ClusterIP
